% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_connorm.r
\name{ci_connorm_max}
\alias{ci_connorm_max}
\title{ci_connorm_max .}
\usage{
ci_connorm_max(
  yk,
  yk1,
  sigma = 1,
  rho = 0,
  p = c(level/2, 1 - (level/2)),
  level = 0.05
)
}
\arguments{
\item{yk}{the observed maximum value, \eqn{y_k}.}

\item{yk1}{a vector of the other observed values, \eqn{y_{k1}}, or just the
scalar second largest value.}

\item{sigma}{the common standard deviation.}

\item{rho}{the common correlation.}

\item{p}{a vector of probabilities for which we return
equivalent \eqn{\eta^{\top}\mu}.}

\item{level}{if \code{p} is not given, we set it by default to
\code{c(level/2,1-level/2)}.}
}
\value{
The values of \eqn{\mu_k} which have the corresponding
CDF.
}
\description{
Confidence intervals on normal mean, conditioning on the max.
}
\details{
Computes the confidence interval of unknown mean of a normal vector
conditional on the one element being the maximum.

Let \eqn{y} be multivariate normal with unknown mean \eqn{\mu}
and known covariance \eqn{\Sigma}. We assume that \eqn{\Sigma}
is compound symmetric with common variance \eqn{\sigma^2} and 
common correlation \eqn{\rho}. 

Conditional on \eqn{y_k \ge y_i}{y_k >= y_i} for all \eqn{i},
we compute the confidence interval of \eqn{\mu_k}.
}
\references{
Lee, J. D., Sun, D. L., Sun, Y. and Taylor, J. E. "Exact post-selection inference, 
with application to the Lasso." Ann. Statist. 44, no. 3 (2016): 907-927.
doi:10.1214/15-AOS1371. \url{https://arxiv.org/abs/1311.6238}
}
\seealso{
the CDF function, \code{\link{pconnorm}}, the MLE function, \code{\link{mle_connorm_max}},
the more general version, \code{\link{ci_connorm}}.
}
\author{
Steven E. Pav \email{shabbychef@gmail.com}
}
